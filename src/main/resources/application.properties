
server.port=8082
spring.application.name=careertracker
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# Email is optional in local development. If SMTP values are not configured,
# emails are printed in the backend terminal instead of being sent.
app.mail.from=PlaceIT Hub <no-reply@placeit.local>
app.frontend.url=http://localhost:8080
# spring.mail.host=smtp.gmail.com
# spring.mail.port=587
# spring.mail.username=your-email@gmail.com
# spring.mail.password=your-app-password
# spring.mail.properties.mail.smtp.auth=true
# spring.mail.properties.mail.smtp.starttls.enable=true
